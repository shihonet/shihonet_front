export interface Schedule {
  name: string;
  urlPath: string;
  startedDate: string;
  categoryName: string;
}

export interface ApiResponseSchedule {
  name: string;
  url_path: string;
  started_date: string;
  category_name: string;
}
